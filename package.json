{
    "name": "go-template",
    "private": true,
    "version": "1.0.0",
    "description": "go-template web config",
    "scripts": {
        "dev:cobotweb": "pnpm --filter cobotweb dev",
        "build:cobotweb": "pnpm --filter cobotweb build",
        "build": "pnpm -r build",
        "dev": "pnpm -r dev"
    },
    "workspaces": [
        "web"
    ]
}